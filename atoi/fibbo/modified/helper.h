long fun(int y); // It will take the value orovided by the user from main function.
long fibbo(int x); //it will take the value from fun() and calculate the fibbonaci series.
long arr[500];	 // An integer array to store the values of fibbo series.
#include<stdio.h> 
#include<time.h>
unsigned int CALL;
